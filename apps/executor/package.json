{
  "name": "executor",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch index.ts",
    "start": "bun index.ts",
    "test": "bun test",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "common": "*",
    "db": "*",
    "lighter-ts-sdk": "^1.1.0",
    "mongoose": "^9.7.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
